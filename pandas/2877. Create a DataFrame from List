import pandas as pd

def createDataframe(student_data: List[List[int]]) -> pd.DataFrame:
    columns_list = ["student_id", "age"]
    result_df = pd.DataFrame(student_data, columns=columns_list)
    return result_df##
